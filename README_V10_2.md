# Daedalus V10.2

La biblioteca se mantiene subiendo PNGs a `assets/marcas`.

Una GitHub Action genera automáticamente `catalogo.json` después de cada cambio en esa carpeta. La aplicación sigue leyendo el catálogo, pero el usuario ya no necesita editarlo manualmente.
