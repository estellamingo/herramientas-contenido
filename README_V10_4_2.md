# Daedalus V10.4.2

Corrección realizada desde la base estable V10.3.

- Se eliminó el selector real `id="dots"`.
- El indicador siempre dibuja tres puntos.
- Se eliminó el listener asociado a `dots`.
- Se conserva la biblioteca sincronizable y el funcionamiento offline.
